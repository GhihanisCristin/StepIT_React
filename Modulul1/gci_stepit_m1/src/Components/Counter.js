import React, {Component} from 'react';

const Counter = (props) => {
    
}
{/*class Counter extends Component {
    state = {
        count: 0
    };
    clickIncrement() {
        this.setState(prevState => ({count: ++prevState.count}))
    }
    clickDecrement() {
        this.setState(prevState => ({count: --prevState.count}))
    }
    render() {
        return (
            <div className="counter">
                <button className="counter-action decrement" onClick={() => this.clickDecrement()}>-</button>
                <span className="counter-score">{this.state.count}</span>
                <button className="counter-action increment" onClick={() => this.clickIncrement()}>+</button>
            </div>
        )
    }
}*/}
export default Counter;
